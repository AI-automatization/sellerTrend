# Railway deploy configuration
# Dockerfile paths and health checks are configured per-service in Railway dashboard.
# This file applies to ALL services deployed via `railway up`.

[deploy]
restartPolicyType = "on_failure"
